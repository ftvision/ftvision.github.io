{
  "_comment": "Copy this file to create a new theme. Create a folder with your theme name and add light.json and dark.json",
  "color": {
    "bg": {
      "primary": { "value": "{primitive.color.white}", "_comment": "Main background" },
      "secondary": { "value": "{primitive.color.gray.50}", "_comment": "Secondary surfaces" },
      "tertiary": { "value": "{primitive.color.gray.100}", "_comment": "Tertiary surfaces" },
      "inverse": { "value": "{primitive.color.gray.900}", "_comment": "Inverse background" }
    },
    "text": {
      "primary": { "value": "{primitive.color.gray.900}", "_comment": "Primary text" },
      "secondary": { "value": "{primitive.color.gray.600}", "_comment": "Secondary text" },
      "muted": { "value": "{primitive.color.gray.400}", "_comment": "Muted/placeholder text" },
      "inverse": { "value": "{primitive.color.white}", "_comment": "Text on inverse bg" }
    },
    "border": {
      "default": { "value": "{primitive.color.gray.200}" },
      "muted": { "value": "{primitive.color.gray.100}" },
      "strong": { "value": "{primitive.color.gray.300}" }
    },
    "action": {
      "primary": { "value": "{primitive.color.blue.600}", "_comment": "Primary buttons/links" },
      "primary-hover": { "value": "{primitive.color.blue.700}" },
      "secondary": { "value": "{primitive.color.gray.100}" },
      "secondary-hover": { "value": "{primitive.color.gray.200}" }
    },
    "status": {
      "info": { "value": "{primitive.color.blue.500}" },
      "info-bg": { "value": "{primitive.color.blue.50}" },
      "success": { "value": "{primitive.color.green.500}" },
      "success-bg": { "value": "{primitive.color.green.50}" },
      "warning": { "value": "{primitive.color.amber.500}" },
      "warning-bg": { "value": "{primitive.color.amber.50}" },
      "danger": { "value": "{primitive.color.red.500}" },
      "danger-bg": { "value": "{primitive.color.red.50}" }
    }
  },
  "font": {
    "family": {
      "heading": { "value": "{primitive.font.family.sans}", "_comment": "serif, sans, or mono" },
      "body": { "value": "{primitive.font.family.sans}" },
      "code": { "value": "{primitive.font.family.mono}" }
    }
  },
  "radius": {
    "default": { "value": "{primitive.radius.md}", "_comment": "none for sharp, md for rounded" },
    "sm": { "value": "{primitive.radius.sm}" },
    "lg": { "value": "{primitive.radius.lg}" },
    "full": { "value": "{primitive.radius.full}" }
  },
  "border": {
    "width": {
      "default": { "value": "1px", "_comment": "Increase for brutalist style" },
      "thick": { "value": "2px" }
    }
  }
}
